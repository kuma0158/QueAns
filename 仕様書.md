# QueAns 仕様書（リバースエンジニアリング版）

本仕様書は `E:\QueAns` 配下の実装コード（`src/`, `prisma/`, `Dockerfile`, `docker-compose.yml`, `vercel.json`, `package.json` 等）のみから読み取れる事実を整理したものです。設計書・要件定義の類は参照していません。

- 対象コミット: `main` ブランチ（`8dfb87e テストユーザー・パスワード表示削除` 時点）
- アプリ名: **QueAns**（`package.json: "name": "queans"`）
- バージョン: `0.1.0`

---

## 1. システムの目的・概要

`src/app/layout.tsx` の `metadata.title` と README から、**社員研修のQ&Aを一元管理する社内ツール**と読み取れる。

実装から確認できる主機能：

| 機能 | 根拠 |
|---|---|
| プロジェクト単位での質問管理（プロジェクトに質問がぶら下がる構造） | `prisma/schema.prisma` の `Question.projectId`、`/projects/[id]/page.tsx` |
| 質問のCRUD（案件名／確認事項／質問者／起票日／期限／回答／回答者／ステータス／備考） | `Question` モデル、`QuestionForm.tsx`、`/api/questions/*` |
| ステータス自動判定（新規／対応中／完了／期限超過） | `src/lib/status.ts` の `resolveDisplayStatus` |
| カテゴリーの多対多分類（色付きバッジ表示） | `QuestionCategory` 中間テーブル、`CategoryBadge.tsx` |
| 添付ファイル（画像はサムネ表示、その他は DL） | `Attachment` モデル、`AttachmentList.tsx`、`/api/attachments/[id]` |
| ダッシュボード（進捗率／プロジェクト別／カテゴリー別／期限超過リスト） | `src/app/dashboard/page.tsx` |
| Cookie ベースの簡易ログイン認証 | `src/lib/session.ts`、`src/middleware.ts` |
| プロジェクト・カテゴリーのCRUD管理画面 | `ProjectManager.tsx`, `CategoryManager.tsx`, `/categories`, `/` |

### 技術スタック

- フロント／サーバ：**Next.js 14.2.18 (App Router)** + React 18 + TypeScript
- スタイル：**Tailwind CSS 3.4**
- ORM／DB：**Prisma 5.22 + PostgreSQL**（`schema.prisma` の `provider = "postgresql"`、Neon 接続を想定）
- 認証：自前のセッションテーブル + `httpOnly` Cookie、パスワードは **bcryptjs**（cost=10、`prisma/seed.ts`）
- 添付ストレージ：**Vercel Blob** または **ローカルファイルシステム**（`BLOB_READ_WRITE_TOKEN` の有無で自動切替）
- デプロイ先候補：**Vercel**（`vercel.json`、リージョン `hnd1`）／**Docker + Caddy**（`Dockerfile`, `docker-compose.yml`, `caddy/`）

---

## 2. データモデル（ER）

`prisma/schema.prisma` から起こしたエンティティと関連。

```
User (1) ────< (N) Session
User (1) ────< (N) Question         （createdBy, onDelete: SetNull）

Project (1) ──< (N) Question        （onDelete: Restrict）
Question (1) ──< (N) Attachment     （onDelete: Cascade）

Question (N) ─< QuestionCategory >─ (N) Category   （両側 onDelete: Cascade）
```

- `Question` と `Category` は **多対多**（中間テーブル `QuestionCategory`、複合主キー `(questionId, categoryId)`）。
- `Question` の親プロジェクトは必須（`projectId Int` 非null）、削除は `Restrict`。
- 質問削除時に紐づく `Attachment` と `QuestionCategory` は `Cascade` で消える。
- `Category` 削除時、`QuestionCategory` は `Cascade` で消えるため、質問は「未分類」に戻る（`CategoryManager.tsx` のメッセージとも一致）。

### 2.1 エンティティと列の意味

#### User（ユーザー）
| 列 | 型 | 既定値／制約 | 意味 |
|---|---|---|---|
| `id` | Int | PK / autoincrement | ユーザーID |
| `username` | String | unique | ログインID |
| `passwordHash` | String | — | bcrypt ハッシュ |
| `displayName` | String | — | 画面表示名（TopNav に表示） |
| `role` | String | 既定 `"user"` | `"admin"` / `"user"`。TopNav で「管理者／一般」と表示 |
| `createdAt` | DateTime | now() | 作成日時 |

権限分岐コードは見当たらず、**ロールはラベル表示にのみ使用**。

#### Session（セッション）
| 列 | 型 | 制約 | 意味 |
|---|---|---|---|
| `id` | String | PK | セッションID（`randomBytes(32).toString("hex")`） |
| `userId` | Int | FK→User, onDelete Cascade | ひも付くユーザー |
| `expiresAt` | DateTime | — | 有効期限。`session.ts` で **7日**固定 |
| `createdAt` | DateTime | now() | 作成日時 |

Cookie 名 `queans_session`、`httpOnly`, `sameSite=lax`, `path=/`。

#### Project（プロジェクト）
| 列 | 型 | 制約 | 意味 |
|---|---|---|---|
| `id` | Int | PK | プロジェクトID |
| `name` | String | unique | プロジェクト名 |
| `description` | String? | — | 説明（任意） |
| `sortOrder` | Int | 既定 0 | 表示順（昇順、次に id 昇順） |
| `createdAt` | DateTime | now() | 作成日時 |

#### Category（カテゴリー）
| 列 | 型 | 制約 | 意味 |
|---|---|---|---|
| `id` | Int | PK | カテゴリーID |
| `name` | String | unique | カテゴリー名 |
| `color` | String | 既定 `"#64748b"` | バッジ色（HEX）。フォームでは `CategoryManager.tsx` のプリセット9色から選択 |
| `sortOrder` | Int | 既定 0 | 表示順 |
| `createdAt` | DateTime | now() | 作成日時 |

#### Question（質問）
| 列 | 型 | 制約 | 意味（UI ラベル） |
|---|---|---|---|
| `id` | Int | PK | 質問ID |
| `projectId` | Int | FK→Project, onDelete Restrict | 所属プロジェクト |
| `content` | String | 必須 | **確認事項**（質問本文） |
| `questioner` | String | 必須 | **質問者** |
| `createdDate` | DateTime | now() | **起票日** |
| `deadline` | DateTime | 必須 | **期限**（フォームは `YYYY-MM-DD`） |
| `answerText` | String? | — | **回答** |
| `answerer` | String? | — | **回答者** |
| `status` | String | 既定 `"new"` | **ステータス**: `new` / `in_progress` / `done` |
| `notes` | String? | — | **備考** |
| `createdById` | Int? | FK→User, onDelete SetNull | 登録者（自動で `getCurrentUser()` から記録、UI 非表示） |
| `updatedAt` | DateTime | @updatedAt | 更新日時 |

インデックス: `status`, `deadline`, `projectId`。

#### QuestionCategory（中間表）
- `(questionId, categoryId)` 複合主キー
- 追加インデックス: `categoryId`

#### Attachment（添付ファイル）
| 列 | 型 | 意味 |
|---|---|---|
| `id` | Int PK | 添付ID |
| `questionId` | Int FK→Question, onDelete Cascade | 紐付く質問 |
| `storedName` | String? | ローカル保存名（`UPLOAD_ROOT/<questionId>/` 配下） |
| `url` | String? | Vercel Blob の URL |
| `originalName` | String | 元ファイル名（DL／表示） |
| `mimeType` | String | MIME |
| `size` | Int | バイトサイズ |
| `createdAt` | DateTime | now() |

**保存先の二択**: `BLOB_READ_WRITE_TOKEN` が設定されていれば Vercel Blob（`url` に格納）、なければサーバのローカル FS（`storedName` に格納）。`/api/attachments/[id]` 配信側は両方をフォールバックで扱う。

---

## 3. ステータスの自動判定ルール

`src/lib/status.ts: resolveDisplayStatus(status, deadline)`：

1. DB の `status === "done"` なら表示は **`done`（完了）**。
2. それ以外で `deadline < 現在時刻` なら **`overdue`（期限超過）**。
3. それ以外は DB の値そのまま（`new` / `in_progress`）。

つまり「期限超過」は**仮想ステータスで、DB には保存されない**。一覧・詳細・ダッシュボードのカウントすべてこのルールに従う。

ステータスごとの日本語ラベルとバッジ配色：

| 内部値 | ラベル | バッジ色 |
|---|---|---|
| `new` | 新規 | 青 (`blue-100/800/300`) |
| `in_progress` | 対応中 | 琥珀 (`amber-100/800/300`) |
| `done` | 完了 | エメラルド (`emerald-...`) |
| `overdue` | 期限超過 | 赤 (`red-...`) |

---

## 4. 画面構成・挙動

ルートレイアウト `src/app/layout.tsx`：
- 全ページ共通の `TopNav`（タブ：「プロジェクト」「ダッシュボード」「カテゴリー管理」）と中央寄せのメインコンテナ。
- `force-dynamic` 指定で SSR、`getCurrentUser()` の結果を TopNav に渡す。

`src/middleware.ts` で `/login` 以外の全パス（および `/api/auth/login` 以外の全 API）は **未ログインなら `/login?redirect=...` へ転送**、API は 401 JSON 返却。

### 4.1 `/login` — ログイン画面
- フォーム: ユーザー名／パスワード
- 送信先: `POST /api/auth/login`
- 成功時はクエリ `redirect`（既定 `/`）へ遷移
- 失敗時メッセージ: 「ユーザー名またはパスワードが違います」

### 4.2 `/`（ルート）— プロジェクト一覧 ＋ プロジェクト管理
`src/app/page.tsx`

- プロジェクトを `sortOrder ASC, id ASC` で取得。
- 各プロジェクトについて、紐づく質問から以下を集計：
  - `total`：件数
  - `done`：`status === "done"` 件数
  - `open`：未完了件数
  - `overdue`：未完了かつ `deadline < now` 件数
  - `progress`：`Math.round(done / total * 100)`（total=0 のとき 0）
- 上段：プロジェクトカードのグリッド（カードクリックで `/projects/[id]` へ）。
  - 進捗バー（emerald）、`進捗 XX%`、`未完了 N`、`超過 M`（M>0 のときのみ赤太字）。
- 下段：`ProjectManager`（新規追加フォームと一覧表）。

### 4.3 `/projects/[id]` — プロジェクト詳細（質問一覧）
`src/app/projects/[id]/page.tsx`

- パンくず：「← プロジェクト一覧」
- ヘッダ：プロジェクト名と説明、右に `+ 新規質問` ボタン（`/questions/new?projectId=...`）
- フィルタ（`QuestionFilters`、クエリパラメータ駆動）：
  - **ステータス**: すべて／新規／対応中／完了／期限超過
    - `overdue` は `status != done AND deadline < now` に変換
  - **カテゴリー**: すべて／登録カテゴリーの各バッジ／未分類（`none` で `categories: { none: {} }`）
  - **検索ボックス `q`**: `content` / `questioner` / `answerer` の OR 部分一致
- テーブル列：`#`, カテゴリー, 確認事項, 質問者, 起票日, 期限, 回答者, ステータス, 編集リンク
- 行はクリックで `/questions/[id]`、`編集` で `/questions/[id]/edit`
- 並び順：`deadline ASC, id DESC`

### 4.4 `/questions/new` — 新規質問
`src/app/questions/new/page.tsx` + `QuestionForm.tsx`

- 既定の期限は **今日 +3 日**
- `?projectId=` で初期プロジェクトをプリセレクト（無効値・該当なしなら最初のプロジェクト）
- バリデーション：プロジェクト必須、確認事項・質問者・期限 HTML 必須属性
- 送信先：`POST /api/questions`、成功で `/projects/[projectId]` に戻る

### 4.5 `/questions/[id]` — 質問詳細（閲覧）
- パンくず：プロジェクト > プロジェクト名 > 質問 #ID
- 右上に「編集する」ボタン
- 表示要素：起票日／期限（超過時は赤太字）／質問者／回答者、確認事項（プレ書式維持）、回答（未回答時は「未回答」と点線枠）、備考、添付ファイル一覧（`AttachmentList`）

### 4.6 `/questions/[id]/edit` — 質問編集
- `QuestionForm`（編集モード、`削除`ボタンあり）と `AttachmentManager`
- 削除確認：「この質問を削除しますか？」、成功で元プロジェクトに戻る

### 4.7 `/dashboard` — ダッシュボード
`src/app/dashboard/page.tsx`

- KPI カード×5：全件数／新規／対応中／完了／期限超過
- **進捗率**：`Math.round(done / total * 1000) / 10` （小数点1桁の％）
- **プロジェクト別 件数**：横棒グラフ（青）。クリックで `/projects/[id]`
- **カテゴリー別 件数**：横棒グラフ（カテゴリー色）
- **期限超過（未完了）**：`status != done AND deadline < now` を期限昇順で最大 **10件**

### 4.8 `/categories` — カテゴリー管理
- 新規追加フォーム（名前、色＝9色プリセット、表示順）
- 一覧表（表示順／カテゴリー名／色／使用件数／編集・削除）
- 削除時の確認文言（使用中件数があるか否かで分岐）：
  - 0 件：「『〇〇』を削除しますか？」
  - N 件：「『〇〇』を削除すると、N件の質問が『未分類』になります。削除しますか？」

### 4.9 TopNav（ヘッダ）
- ロゴ：「QueAns 社員研修QA管理」
- タブはログイン中のみ表示
- ユーザー名と役割バッジ、ログアウトボタン

---

## 5. REST API

すべて Next.js App Router の Route Handler。`/api/auth/login` 以外は `middleware.ts` でログイン必須。

### 5.1 認証

| メソッド | パス | 概要 |
|---|---|---|
| POST | `/api/auth/login` | `{ username, password }` を受け、bcrypt 検証 → セッション作成、Cookie 発行。レスポンス `{ user: { id, username, displayName, role } }`。失敗時 400/401 |
| POST | `/api/auth/logout` | セッション削除、Cookie 破棄、`{ ok: true }` |

### 5.2 Project

| メソッド | パス | 概要 |
|---|---|---|
| GET | `/api/projects` | 一覧（`sortOrder, id` 昇順、`questionCount` を含む） |
| POST | `/api/projects` | 新規作成。`name` 必須、`name` ユニーク違反は 409。`description` は空文字なら null |
| PATCH | `/api/projects/[id]` | 部分更新（`name`/`description`/`sortOrder`）。同名は 409 |
| DELETE | `/api/projects/[id]` | 紐付く質問が 0 件のときのみ削除（>0 なら 409、メッセージ付き） |

### 5.3 Category

| メソッド | パス | 概要 |
|---|---|---|
| GET | `/api/categories` | 一覧（`questionCount` を含む） |
| POST | `/api/categories` | 新規作成。`name` 必須、`color` 任意（既定 `#64748b`）、同名 409 |
| PATCH | `/api/categories/[id]` | 部分更新（`name`/`color`/`sortOrder`）、同名 409 |
| DELETE | `/api/categories/[id]` | 削除（FK は Cascade）。紐付き有無に関わらず削除可 |

### 5.4 Question

| メソッド | パス | 概要 |
|---|---|---|
| GET | `/api/questions` | クエリ `status` / `q` / `category` / `project` で絞り込み一覧。`status=overdue` は `status != done AND deadline < now` に変換。`category=none` は未分類 |
| POST | `/api/questions` | 作成。必須: `projectId, content, questioner, deadline`。`status` は許可値以外なら `new`。`categoryIds` は数値配列で中間表に同時作成。`createdById` はサーバ側で現在ユーザーをセット |
| GET | `/api/questions/[id]` | 単体取得（project, categories 含む） |
| PATCH | `/api/questions/[id]` | 部分更新。文字列項目は空文字なら null。`categoryIds` が配列で来た場合は中間表を一括差し替え（トランザクション） |
| DELETE | `/api/questions/[id]` | 削除（添付・中間表は Cascade） |

検索の `q` は **大文字小文字や全文索引の指定なし** の `contains` 部分一致（プロジェクト詳細では content/questioner/answerer、`/api/questions` GET ではさらに `project.name` も含む）。

### 5.5 Attachment

| メソッド | パス | 概要 |
|---|---|---|
| GET | `/api/questions/[id]/attachments` | 質問の添付一覧 |
| POST | `/api/questions/[id]/attachments` | `multipart/form-data`、フィールド名 `files`（複数可）。**1ファイル最大 10MB**（`MAX_FILE_SIZE`）超過で 413。`BLOB_READ_WRITE_TOKEN` ありなら Vercel Blob に `questions/<qid>/<storedName>` で保存し DB の `url` に記録。無ければ `UPLOAD_ROOT/<qid>/<storedName>` に書き出し `storedName` に記録。`storedName` は `<unix_ms>_<random_hex>.<ext>` |
| GET | `/api/attachments/[id]` | 認証ゲートで配信。`url` があれば Blob を fetch、無ければ FS から読み出し、`Content-Type` / `Content-Length` / `Content-Disposition: inline\|attachment; filename*=UTF-8''<encoded>` を返す。`?download=1` で attachment |
| DELETE | `/api/attachments/[id]` | Blob または FS の実体を削除し、DB レコード削除 |

---

## 6. 認証・セッション

- ハッシュ：`bcryptjs` cost=10（`prisma/seed.ts`）
- セッション保存：DB の `Session` テーブル + Cookie `queans_session`（`httpOnly`, `sameSite=lax`, `path=/`、有効期限 7 日 = `SESSION_DURATION_MS = 7 * 24 * 3600 * 1000`）
- `getCurrentUser()` で Cookie → セッション → ユーザー解決。期限切れは検出時に DB から削除し `null` を返す
- `/api/auth/logout` で DB セッション削除＋Cookie 破棄
- ロールベース認可ロジックは実装されておらず、**ログイン済みなら全 CRUD 可能**

`middleware.ts` のパブリックパス：`/login`, `/api/auth/login` （およびその配下）

---

## 7. ファイルアップロード仕様

- 上限：`MAX_FILE_SIZE = 10MB`
- アップロード後のファイル名（保存名）：`${Date.now()}_${randomBytes(8).hex}${元の拡張子}`
- 画像判定：`mimeType.startsWith("image/")`（`AttachmentList`/`AttachmentManager` でサムネ表示）
- 画像以外：📎 アイコンとダウンロードボタン（ダウンロードは `?download=1`）
- 保存先の判定：`process.env.BLOB_READ_WRITE_TOKEN` の有無
- ローカル保存ディレクトリ：`UPLOAD_ROOT`（既定 `process.cwd()/uploads`）
- 配信は **常に `/api/attachments/[id]` 経由**（middleware による認証ゲートを通る）

---

## 8. 環境変数

| 変数 | 用途 | 既定値 | 必須 |
|---|---|---|---|
| `DATABASE_URL` | Prisma の接続文字列 | — | **必須** |
| `DATABASE_URL_UNPOOLED` | Prisma の `directUrl`（マイグレーション用） | — | 任意（Neon 等） |
| `BLOB_READ_WRITE_TOKEN` | Vercel Blob 利用時のトークン | 未設定なら FS フォールバック | 任意 |
| `UPLOAD_ROOT` | ローカル保存先 | `cwd()/uploads` | 任意 |
| `SESSION_SECRET` | docker-compose で必須化されているが、`src/` 中では参照無し（互換目的の予約変数とみられる） | — | docker-compose では必須 |
| `SEED_ADMIN_PASSWORD` / `SEED_USER_PASSWORD` | docker-compose 環境変数（`prisma/seed.ts` 内では現状ハードコード `admin123` / `user123` を使用） | `admin123` / `user123` | 任意 |
| `DOMAIN` / `LETSENCRYPT_EMAIL` | Caddy（Docker 運用時のドメインと Let's Encrypt 連絡先） | — | docker-compose で必須 |

> 注：README には `SEED_ADMIN_PASSWORD` を seed が使うように記述があるが、現状の `prisma/seed.ts` は `bcrypt.hash("admin123", 10)` をリテラルで使用しており、環境変数は参照していない（コード上の事実）。

---

## 9. 初期データ（seed）

`prisma/seed.ts` を実行すると以下が投入される（`upsert` のため繰り返し実行しても重複しない）。

### 9.1 ユーザー
| username | displayName | role | パスワード（ハッシュ前） |
|---|---|---|---|
| `admin` | 管理者 | `admin` | `admin123` |
| `user` | 一般ユーザー | `user` | `user123` |

### 9.2 カテゴリー
| 名前 | 色 | sortOrder |
|---|---|---|
| 仕様 | `#f97316` | 10 |
| バグ | `#ef4444` | 20 |
| 技術 | `#10b981` | 30 |
| 運用 | `#8b5cf6` | 40 |
| その他 | `#64748b` | 90 |

### 9.3 プロジェクト
- `新人研修2026`（説明: 2026年度新卒エンジニア研修, sortOrder 10）
- `OJT 第一期`（説明: 現場配属後のフォローアップ, sortOrder 20）

### 9.4 サンプル質問（`questionCount === 0` のときのみ作成）
1. `[新人研修2026]` PostgreSQL のインデックス設計について教えてください。（期限 +3日、技術）
2. `[新人研修2026]` Git の rebase と merge の使い分けは？（期限 +1日、in_progress、回答あり、技術）
3. `[OJT 第一期]` 本番DBへの DDL を流す際の事前手順を共有してほしい。（期限 -2日＝期限超過、運用＋技術、備考「緊急度高」）
4. `[OJT 第一期]` TypeScript の satisfies と as の使い分けが分かりません。（期限 -5日、status `done`、回答あり、技術＋仕様）

---

## 10. 起動・運用方法

`package.json` の `scripts`:

| スクリプト | コマンド | 用途 |
|---|---|---|
| `dev` | `next dev` | 開発サーバ（既定 `http://localhost:3000`） |
| `build` | `next build` | 本番ビルド |
| `start` | `next start` | 本番サーバ起動 |
| `lint` | `next lint` | Lint |
| `db:migrate` | `prisma migrate dev --name init` | ローカル マイグレーション作成＋適用 |
| `db:deploy` | `prisma migrate deploy` | 既存マイグレーションを本番適用 |
| `db:push` | `prisma db push` | スキーマを直接 push |
| `db:seed` | `tsx prisma/seed.ts` | 初期データ投入 |
| `postinstall` | `prisma generate` | クライアント生成 |

### 10.1 ローカル開発（README より）

```powershell
cd e:\QueAns
npm install
npx prisma migrate dev --name init
npm run db:seed
npm run dev
```

初期ログイン：`admin / admin123`（seed 値）

### 10.2 Vercel 本番デプロイ

`vercel.json`:
- フレームワーク：`nextjs`
- `buildCommand`: `prisma generate && prisma migrate deploy && next build`
- `installCommand`: `npm install`
- `regions`: `["hnd1"]`（東京）

連携が想定されている Vercel Storage：
- **Neon (Postgres)** — `DATABASE_URL` を自動注入
- **Vercel Blob** — `BLOB_READ_WRITE_TOKEN` を自動注入（あれば添付は Blob に保存）

### 10.3 Docker + Caddy 運用

`Dockerfile`（multi-stage、Node 20-bookworm-slim）：
1. deps：`npm ci`（`prisma` ディレクトリも同梱して `postinstall` でクライアント生成）
2. build：`prisma generate` → `next build`
3. runtime：`.next`, `public`, `node_modules`, `prisma`, `package*.json` をコピー、`/data` をボリューム、`tini` 経由で
   `npx prisma migrate deploy && npx tsx prisma/seed.ts && next start` を実行
- 公開ポート：`EXPOSE 3000`

`docker-compose.yml`：
- `app` サービス
  - `DATABASE_URL=file:/data/db/queans.db`（SQLite 形式の文字列だが、現行 `schema.prisma` の provider は `postgresql` なので、SQLite 運用するにはスキーマを `sqlite` に戻す必要がある — README 末尾「VPS + Docker」セクションでも同じ注意書きあり）
  - `UPLOAD_ROOT=/data/uploads`
  - `SESSION_SECRET` 必須、`SEED_ADMIN_PASSWORD` / `SEED_USER_PASSWORD` も渡せる
  - ボリューム `./data:/data`
- `caddy` サービス
  - `caddy:2` イメージ、`80/443` 公開、`./caddy/Caddyfile` 読込
  - `DOMAIN` と `LETSENCRYPT_EMAIL` 必須

### 10.4 スキーマ変更時の流れ（README）

```powershell
npx prisma migrate dev --name <name>
git add prisma/migrations
git commit -m "schema: <name>"
git push
```

Vercel ビルド時に `prisma migrate deploy` が走るため push だけで本番に反映される。

---

## 11. 主要な実装上の挙動メモ

- すべての一覧クエリは `sortOrder ASC, id ASC` あるいは `deadline ASC, id DESC` 等で**安定ソート**される。
- 質問一覧の検索は `contains` 部分一致のみで、AND 検索や全文索引は無し。
- 添付 GET は `Cache-Control: private, max-age=0, must-revalidate` でブラウザキャッシュ不可。
- `Content-Disposition` のファイル名は `filename*=UTF-8''<URL-encoded>` 形式で日本語名対応。
- フィルタリンクは `useSearchParams` に基づきクエリを差し替える形（既存パラメータを維持）。
- `force-dynamic` がレイアウトと `/api/stats` に付与され、SSR 時のキャッシュを抑止。
- DB レイヤは `globalThis.prisma` を再利用し、`NODE_ENV !== "production"` のときグローバルに保持（Next.js dev HMR でのコネクション漏れ対策、定石）。

---

## 12. ファイル構成（参考）

```
src/
  middleware.ts               認証ゲート
  app/
    layout.tsx                共通レイアウト（TopNav + main）
    page.tsx                  プロジェクト一覧 + 管理
    login/page.tsx            ログイン画面
    dashboard/page.tsx        ダッシュボード
    categories/page.tsx       カテゴリー管理
    projects/[id]/page.tsx    プロジェクト詳細＝質問一覧
    questions/new/page.tsx    新規質問
    questions/[id]/page.tsx   質問詳細
    questions/[id]/edit/page.tsx 質問編集
    api/
      auth/login   POST
      auth/logout  POST
      projects     GET / POST
      projects/[id]            PATCH / DELETE
      categories   GET / POST
      categories/[id]          PATCH / DELETE
      questions    GET / POST
      questions/[id]           GET / PATCH / DELETE
      questions/[id]/attachments  GET / POST
      attachments/[id]            GET / DELETE
      stats        GET
  components/
    TopNav, QuestionForm, QuestionFilters,
    StatusBadge, CategoryBadge,
    ProjectManager, CategoryManager,
    AttachmentList, AttachmentManager
  lib/
    db.ts                Prisma クライアント
    session.ts           セッション作成/破棄/取得
    status.ts            ステータス表示判定
    uploads.ts           UPLOAD_ROOT, MAX_FILE_SIZE, パス組み立て
    attachment-utils.ts  isImageMime, formatBytes
prisma/
  schema.prisma
  seed.ts
  migrations/20260531053841_init/...
Dockerfile / docker-compose.yml / caddy/Caddyfile
vercel.json
```

---

## 13. 既知の不整合・注意点（コードから読み取れる範囲）

1. `docker-compose.yml` の `DATABASE_URL` は SQLite 形式（`file:/data/db/queans.db`）だが、`prisma/schema.prisma` の `provider` は `postgresql` 固定。Docker 運用するには Postgres コンテナを追加するか、`schema.prisma` を `sqlite` に戻す必要がある（README にも同様の注記あり）。
2. `prisma/seed.ts` は `SEED_ADMIN_PASSWORD` / `SEED_USER_PASSWORD` を**読んでいない**（リテラルの `admin123` / `user123` を bcrypt）。README の説明と乖離している。
3. ロール (`User.role`) は表示のみで、認可ロジックには使われていない（ログイン済みなら全員が CRUD 可能）。
4. `category` の `onDelete` は **Cascade**（`QuestionCategory` 経由で関連は消えるが、質問本体は削除されない）。`src/app/api/categories/[id]/route.ts` のコメントには「SetNull」と書かれているがコメント上のみで、実スキーマは Cascade。

（以上、コードに記述されている事実のみを記載）
